apiVersion: v1
kind: Service
metadata:
  name: {{ include "giddy1.fullname" . }}
  labels:
    {{- include "giddy1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "giddy1.selectorLabels" . | nindent 4 }}
